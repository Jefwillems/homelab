# The wrenix forgejo-runner chart is sunset; the chart aborts rendering unless you
# explicitly acknowledge you are on its last released version.
knownLastVersion: true

runner:
  config:
    existingInitSecret: forgejo-actions-secret
    file:
      runner:
        labels:
          - homelab-runner
