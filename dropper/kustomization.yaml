apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: dropper

helmCharts:
  - name: dropper
    repo: oci://ghcr.io/jefwillems/charts
    version: 1.2.0
    releaseName: dropper
    valuesFile: values.yaml
