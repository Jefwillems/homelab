apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: media-data
  namespace: media
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: ''
  volumeName: media-ssd-data
  resources:
    requests:
      storage: 900Gi
