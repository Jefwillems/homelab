# $schema: https://github.com/aclerici38/pocket-id-operator/releases/latest/download/pocketidoidcclient_v1alpha1.json
apiVersion: pocketid.internal/v1alpha1
kind: PocketIDOIDCClient
metadata:
  name: technitium
spec:
  callbackUrls:
    - 'https://dns.jef.app/sso/callback'
  logoutCallbackUrls:
    - 'https://dns.jef.app/sso/callback'
  pkceEnabled: true
  requiresReauthentication: false
  launchUrl: 'https://dns.jef.app/'
  allowedUserGroups:
    - name: platform-owners
      namespace: pocket-id
  secret:
    name: technitium-oidc
    additionalLabels:
      app.kubernetes.io/part-of: technitium
    keys:
      clientID: client_id
      clientSecret: client_secret
      issuerUrl: issuer_url
      discoveryUrl: discovery_url
